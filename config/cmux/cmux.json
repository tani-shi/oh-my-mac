{
  "commands": [
    {
      "name": "Claude Session",
      "keywords": ["claude", "dev"],
      "workspace": {
        "layout": {
          "direction": "vertical",
          "split": 0.6,
          "left": { "panes": [{ "surfaces": [{ "command": "claude" }] }] },
          "right": { "panes": [{ "surfaces": [{ "command": "zsh" }] }] }
        }
      }
    }
  ]
}
