# Codex instructions

## Git

- Name branches for the change with a type prefix; avoid `codex/`.

## Sandbox

- When authentication fails in the sandbox, verify it outside the sandbox before treating credentials as invalid or switching tools.
