interface:
  display_name: "Deliver Change"
  short_description: "Deliver one change through PR or merge"
  default_prompt: "Use $deliver-change with an objective to prepare and merge one change when repository gates allow it."

policy:
  allow_implicit_invocation: false
