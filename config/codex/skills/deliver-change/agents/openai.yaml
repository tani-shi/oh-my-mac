interface:
  display_name: "Deliver Change"
  short_description: "Start or resume a supervised change delivery"
  default_prompt: "Use $deliver-change to start or continue the supervised delivery in this task."

policy:
  allow_implicit_invocation: false
